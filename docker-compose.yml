version: '3.8'

services:
  web-management-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web-management-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
