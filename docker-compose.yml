version: '3.8'

services:
  uzx-finance-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: uzx-finance-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=uzx_production_secret_key_cairo_finance_2026_secure
      - DATA_DIR=/app/data
    volumes:
      - uzx_db_data:/app/data

volumes:
  uzx_db_data:
    driver: local
